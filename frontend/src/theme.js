import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00796b',
      light: '#00897b',
      dark: '#004d40',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;
